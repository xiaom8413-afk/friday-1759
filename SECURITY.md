# 安全与隐私

游戏是静态 HTML、CSS、JavaScript，运行不调用 AI、不连接后端、不包含账号登录和统计上报。音效由 Web Audio 生成，美术由内联 SVG 和 Canvas 绘制。

进度使用当前浏览器的 localStorage。它不是加密存储，也不是云存档。直接打开 HTML 与通过本地 HTTP 服务访问可能拥有不同的存档空间；清除站点数据会丢失进度。发布仓库不包含用户存档。

如发现脚本注入、意外联网或存档解析问题，请先通过 GitHub 联系维护者，避免在公开 Issue 中暴露个人信息。推荐使用现代浏览器；下载后可用 Release 附件中的 SHA-256 校验值核对文件。
