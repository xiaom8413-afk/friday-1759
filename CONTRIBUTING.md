# 参与贡献

感谢体验《周五 17:59》。欢迎通过 Issue 反馈体验，也欢迎提交小而明确的改进。

## 开发

运行游戏不需要依赖。修改 `engine.js`、`renderer.js`、`game.js`、`systems.js`、`art.js`、`style.css` 或 `index.html`，然后：

```sh
node --test tests/*.test.cjs
node build-standalone.cjs
```

逻辑变化应有针对性测试；UI 修改请附实际截图。不要直接修改生成文件 `周五1759.html`，应从源文件重新打包。保留双击离线运行、无外部运行依赖的特性。

## 适合贡献的方向

- 不破坏连通性的地图设计、清晰可辨的状态反馈。
- 基于通关过程和战报的数值平衡建议。
- 键盘操作、窄屏布局和不同浏览器兼容性。
- 存档兼容性和边界条件测试。

请说明复现步骤、操作系统、浏览器版本、关卡和波次。不要上传个人浏览器数据或完整用户目录。大型玩法调整请先用 Issue 讨论，避免无效投入。
